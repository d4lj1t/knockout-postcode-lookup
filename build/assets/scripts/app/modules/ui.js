/**
 * Small UI changes should be handled in here
 */
define(['jquery', 'breakpoints'], function($, breakpoints) {
	
	var module = (function() {

		var my = {};

		my.init = function() {
		};

		return my;

	})();

	return module;
});